{% import '@SymfonyAdmin/layout/backend/macros.html.twig' as LUI %}

{% set items = items|default([])|merge([
  {
    path: path('security_logout'),
    name: 'Log Out'
  }
]) %}

{{ LUI.navigation(items) }}
